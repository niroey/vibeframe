// ---------------------------------------------------------------
// AI 연동 설정
// ---------------------------------------------------------------
// 보안상 API 키는 프론트엔드 코드에 절대 넣지 않습니다.
// 실제 AI 기능을 쓰려면 /server 폴더의 백엔드를 실행하고,
// server/.env 파일에 ANTHROPIC_API_KEY 값을 채워 넣으세요.
//
// 아래 enabled 값은 "백엔드(/api/ai/edit)에 요청을 시도할지 여부"만
// 결정합니다. 백엔드가 꺼져 있거나 키가 비어 있으면 자동으로
// aiService.js 내부의 로컬 규칙 기반 시뮬레이터로 대체됩니다.
// ---------------------------------------------------------------

export const aiConfig = {
  // 백엔드 프록시 사용 여부. 기본은 false(로컬 시뮬레이션만 사용).
  // server/ 를 실행하고 키를 채운 뒤 true로 바꾸면 실제 Claude API를 사용합니다.
  useRemoteBackend: false,

  // 프론트엔드에서 직접 호출할 백엔드 프록시 경로 (server/server.js 참고)
  backendEndpoint: '/api/ai/edit',

  // 이 값은 참고용일 뿐, 실제 키는 절대 여기 넣지 마세요.
  apiKeyNote: '키는 server/.env 의 ANTHROPIC_API_KEY 에만 설정합니다.'
}
